# Отслеживание курса валют 💱📈

Выполнение тестового задания в компанию Газпром ИТ

анный проект реализует функционал для отслеживания курсов валют, где пользователи могут выбрать одну из валют (доллар, евро или юань) и увидеть график изменения курса за определённый период времени. Проект использует API для получения данных и отображает график с помощью библиотеки ECharts 📊.

## Как запустить ⚡
- Установите зависимости из корня репозитория (Нужна установленная Node.js v14 и выше):
  ```bash
  npm install
  ```

- Запускаем сервис:
  ```bash
  npm run start
  ```

## Инфраструктура 🔧:
- `Webpack ` –  (через react-scripts) — для сборки проекта.
- `Babel` — для тестирования компонентов.
- `Jest ` – для проверки кода и единого стиля
- `TypeScript  ` — для статической типизации и улучшения разработки.
- `Create React App (CRA)` — для упрощенной настройки сборки и старта проекта, включает конфигурации Webpack, Babel и других инструменто